// Blog Entry
class BlogEntry {
    constructor(inTitle, inDate, inSummary) {
        this.title = inTitle;
        this.date = inDate;
        this.summary = inSummary;
    }
}

export default BlogEntry